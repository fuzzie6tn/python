More on turtle graphics, event listeners, state, and multiple instances(objects)

games we are going to make:
-> etch a sketch
-> turtle race

Event listeners: user presses a key and what will happen. (screen events)
.listen() this allows the turtle screen to listen to the event and waiting for the event to trigger. like tapping
on a key
